#!/usr/bin/env php                       
<?php                                    
                                         
$port = isset($argv[1]) ? $argv[1] : 8000; 
                                         
exec("php -S localhost:$port -t public");